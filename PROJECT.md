# PROJECT.md: barum 프로젝트 문서

> 프로젝트 정의·아키텍처·확정 결정을 담는 **안정 문서**.
> 가변 상태(진행·할 일·데이터)는 `ROADMAP.md`, 작업 규칙은 `CLAUDE.md`, 도메인 정의는 `reference/`(예정).
> **여기의 "확정 결정"은 함부로 되돌리지 않는다.**

---

## 1. 프로젝트 정의
- **barum**: 이커머스 상품 상세페이지의 **과대·부당광고를 VLM + 자체 DL로 판정**하는 에이전트.
- **지향 범위:** 전 카테고리 이커머스 부당광고. **현재 기능화(선택 가능) 범위 = 식품(건강기능식품 포함).** 첫 vertical = 다이어트 보조제.
- **규제 틀:** 식품 표시·광고법(식약처). 카테고리 확장 시 규제 도메인별 코퍼스 추가(화장품법·의료기기법 등). **멀티 품목으로 설계하고 식품 슬롯만 채운다.**
- **정체성:** 단발 탐지가 아니라 **닫힌 순환**(탐지→검토→조치→추적→재학습). **판단은 사람, 순환·개선·행동은 에이전트.** (워크플로우 문서 참조, §7)

---

## 2. 확정 결정 (되돌리지 말 것)
1. **판정 방식 = VLM.** raw 이미지에 CNN 단독 학습 ❌. 과대광고는 "이미지 속 글자(광고 문구)를 읽고 식약처 기준 위반인지"라 사실상 OCR/텍스트 문제.
2. **DL/ML 전략 = 지식 증류(선생–학생).** VLM(Claude/Gemini) = **자동 라벨러 + 베이스라인(선생).** 우리 DL 모델 = **학생 = 코어 산출물.**
   - **DL 코어 = ① + ②**
     - **① OCR → 한국어 텍스트 분류기**(문장 다중클래스) = **위반 판정 코어이자 재학습 대상.**
     - **② 이미지유형 CNN**(전이학습) = **정적 CV 라우터**(인증서·후기·성분표·제품컷 세그먼트). "이미지 DL 요건" 충족용, **재학습 루프에서는 제외.**
   - **F18 '학습시키기' 버튼 = ①만 재학습.** **사람 승인 라벨만** 학습에 사용(자동 라벨 자기학습 금지, 오염 방지 가드레일).
3. **크롤 = 11번가**(`view-desc` 순수 requests, 안티봇 약함). 네이버·쿠팡은 후순위.
4. **비용은 병목 아님.** 학습 컴퓨트 ~$0(Colab T4). 실질 비용은 **VLM 라벨링 + OCR**. 진짜 병목은 데이터 확보 + 식약처 레퍼런스 품질.
5. **정책:** **recall 우선**(미탐 비용 > 오탐 비용). UI는 점수가 아니라 **근거 개수**로 표현.
6. **데이터 저장 = 3층 + Supabase.** (2026-08-07 인터뷰 확정, 상세 §3.5)
   - 레퍼런스(법령·인정문구)=`reference/` MD + OpenCrab / 운영 raw(이미지·타일)=파일시스템 / **학습·운영 데이터=Supabase Postgres**(무료 별도 프로젝트).
   - **수집 정책 = 연구목적 소량**(crawl-delay ≥ 1s), **개인정보 최소수집**(대표자명 등 미수집). 11번가 robots는 화이트리스트 봇 외 UA에 형식상 `Disallow: /`이나 연구목적 제한수집으로 진행. **서비스화 시 정식 허가 재검토.**
   - **MVP 범위는 실무자 웹 검토(B)까지.** 로그인 필요, 역할은 reviewer/admin 2단계.

---

## 3. DL 코어 ① 설계 (2026-08-04 인터뷰 확정)

| 항목 | 결정 |
|---|---|
| 판정/입력 단위 | **문장/표현** |
| 출력 | **다중클래스 1라벨** (현행 = 호수 체계, `reference/violation_types/` 기준): {합법, 1호_질병표방, 2호_의약품오인, 3호_건기식오인, 4호_거짓과장, 5호_소비자기만, 대상외} ※ 2026-08-07 정정: 아래 3-1의 옛 6종(후기 효과보증·안전성 단정 등)은 폐기, 실제 라벨링·reference와 일치시킴 |
| 부트스트랩 라벨 | VLM **span 추출**. 전체 문맥을 읽고 위반 표현+유형을 뽑음, 안 뽑힌 문장은 합법. **근거 span 확보** |
| 학생 입력 | **문장(span) + 주변 문맥 윈도우**(지금) / **+ 원료 메타 슬롯**(Phase 2, reference ㉯ 이후 채움) |
| OCR | **로컬 무료 먼저 → 품질 미달 시 CLOVA 폴백**(F18 데이터 품질 게이트로도 사용) |
| 회피표기 정규화(⑤) | **룰 기반 사전(코어, 관리자 갱신)** + 여유 시 데이터 증강 |
| 백본 | **출발 = KoELECTRA-base** → 정답셋으로 KLUE-RoBERTa 등 **후보 실측 비교해 최종 확정** |
| VLM provider | **Gemini(무료) 시작**, 얇은 어댑터로 Claude 교체 가능. 최종은 정밀도/재현율·비용 실측 |
| 클래스 불균형 | 질병표방 편중(적발의 63.6%) → class weight 등으로 조정(실측 시) |

### 3-1. 입력 스키마 (한 학습 샘플)
```
[문장/근거 span]        ← 판정의 주어. 그대로 "근거 N건" UI로 출력
+ [주변 문맥 윈도우]     ← 앞뒤 1~2문장 (지금)
+ [원료 메타 슬롯]       ← 원료명·인정원료 여부(reference ㉯ 매칭) (Phase 2, 비파괴 확장)
→ 라벨: {합법 / 위반유형}
```
- **Phase 1:** 문장(span) + 주변 문맥으로 학습·평가 (reference 의존 없음, 바로 착수).
- **Phase 2:** + 원료 메타 슬롯 채워 재학습 → 합법 인정문구 오판을 정밀하게 제거.

### 3-2. 미탐 회수 캐스케이드 (정답셋 구축 + 운영 F19/F15 공용)
- **1차** VLM 최초 판정 → {위반, 합법}
- **2차** 1차의 "합법"을 **다른 모델**로 재판정(자동, 저비용) → 놓친 위반 회수. = 워크플로우 **F19**
- **3차** 2차 후에도 "합법"인 것에서 **표본추출 → 관리자(사람) 최종 판단.** = **F15 / L4 회수 루프**
- 왜 다른 모델: 같은 모델이면 같은 실수를 반복해 미탐을 못 잡음(판정자 상관 낮추기).
- **학습셋**(대량)은 캐스케이드로 라벨 품질↑. **평가 홀드아웃**은 3차 사람 표본 비중↑. **미탐율 최종 기준은 사람.**

---

## 3.5 데이터 저장 아키텍처 (2026-08-07 인터뷰 확정)

**3층 구조. 자료 성격별로 그릇이 다르다:**
| 층 | 그릇 | 비고 |
|---|---|---|
| 레퍼런스 지식 | `reference/` MD + OpenCrab | 이미 구축 |
| 운영 raw(이미지·타일) | 파일시스템 → (웹단계) 오브젝트 스토리지 | |
| 학습·운영 데이터 | **Supabase Postgres**(무료 별도 프로젝트) | 관계형·JSONB·pgvector |

**DB 선택 근거:** 데이터가 강한 관계형(product→sentence→label)이라 문서형(Mongo) 부적합. Postgres=JSONB(반정형 메타)+pgvector(임베딩 여지). 웹 다중사용자라 BaaS(Supabase=인증·스토리지·API 무료 묶음). SQLite는 다중 쓰기 불가로 폐기.

**7테이블** (상세 스키마 = `schema.sql`, 필드 명세 = `docs/metadata_spec.md`):
`sellers` · `products` · `listings` · `detail_images` · `sentences` · `labels` · `users` + `current_labels` 뷰.
- **개체 3층:** **product**(제품 identity·중복식별 키=품목제조신고번호) / **listing**(셀러 판매글·조치 단위) / **creative=detail_images**(광고물·판정 단위).
- **중복제거:** 재분석 스킵 단위 = 광고물(이미지 `sha256`). 제품이 같아도 광고물이 다르면 재분석(recall 우선).
- **라벨:** append-only. `label_source`∈{vlm_hint, human_initial, staff_review}, `approved_for_training`. **사람 승인 라벨만 학습**(vlm_hint 제외)한다. 가드레일이다. 실무자(staff_review) 라벨도 학습에 포함한다.
- **증거보존:** 원본 이미지 파일 + `sha256` + `crawled_at` 삼각으로 성립. DB엔 메타만, 실제 증거는 보관 파일. MVP는 이미지+해시만(HTML 스냅샷 X).
- **주요 광고문구:** 별도 저장 없이 `labels ⨝ sentences` 조인 렌더.

**초기 라벨 원본 = xlsx**(사람 라벨링, 2026-08-08 완료 예정) → DB 임포트. 운영 실무자 라벨이 쌓이며 DB가 뷰→원본으로 전환.

---

## 4. 아키텍처 / 파이프라인 (목표)
```
수집(11번가 view-desc) → 타일 분할 → OCR → 회피표기 정규화(⑤)
  → VLM 라벨링(span 추출)  → [학생 ① 학습: 문장 다중클래스]
  → 평가(vs VLM 베이스라인, 미탐율 1급) → 미탐 회수 캐스케이드(F19/F15) → F18 재학습
② 이미지유형 CNN = 정적 라우팅 보조.
```
- **기존 수집·타일분할·중복제거는 그대로 재사용.** 기존 "VLM 판정 스크립트" 구상은 폐기가 아니라 **자동 라벨러로 재활용.**
- **코드 구조 목표:** 얕은 패키지 `src/barum/`(`crawl/ preprocess/ judge/ reference/`), 실행은 `scripts/`·`python -m`, 모델 호출은 **얇은 어댑터**. (현재는 평면 스크립트, 이관 현황은 `ROADMAP.md`)

---

## 5. 파일·실행
- 현재 스크립트·실행 명령·데이터 현황은 **`ROADMAP.md` 참조**(가변). 구조가 안정화되면 이 문서로 이관.

## 6. 규제 근거 / 도메인 정의
- **㉮ 위반유형 스키마**, **㉯ 식약처 인정문구·부당광고 금지유형** → `reference/`(예정, Markdown).

## 7. 관련 외부 문서
- **DL/ML 전략 원본:** `temp/HANDOFF_딥러닝전략.md`
- **제품 워크플로우**(F18/F19·레인·2축 우선순위): `sasac_pjt/final-project/7.워크플로우_최종.md`
