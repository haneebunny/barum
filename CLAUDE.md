# CLAUDE.md: barum 작업 규칙

이 파일은 **Claude가 이 프로젝트에서 읽고 따라야 할 작업 규칙**만 담는다.
프로젝트 정의·아키텍처·실행 명령·규제 근거 같은 "사용법/레퍼런스"는 여기 두지 않는다(→ 문서 지도 참조).

## 문서 지도
- **CLAUDE.md** (이 파일): 내가 따라야 할 작업 규칙.
- **ROADMAP.md** (가변 상태): 진행상황·우선순위 할 일·데이터 현황. (기존 `HANDOFF.md`를 개명)
- **PROJECT.md**: 프로젝트 정의·아키텍처·규제 근거, 그리고 확정 결정(VLM 방식·11번가 크롤·DL 코어 전략 등, 되돌리지 말 것). **결정 사항은 이 문서를 참조한다.**
- **reference/** *(예정, Markdown)*: 이 프로젝트 고유의 도메인 정의. ㉮ 위반유형 스키마, ㉯ 식약처 레퍼런스(합법 인정문구 + 부당광고 금지유형).

---

## A. 착수 규칙 (기능·버그를 시작할 때)
- 코드보다 먼저 나를 인터뷰한다.
- 질문은 한 번에 하나씩, 중요한 것부터.
- 내가 안 정한 결정은 선택지(2~3개)를 트레이드오프와 함께 제시하고 승인을 기다린다. 대신 정하지 않는다.
- 모르는 건 추측하지 말고 멈춰서 묻는다.

## B. 진행 규칙
- 구현은 **단계별 계획을 먼저** 보여준다. 각 단계에서 어떤 파일을 어떻게 바꿀지.
- 내가 **"진행"이라고 말하기 전엔 코드를 쓰지 않는다.**
- 확신이 안 서는 지점을 추측으로 채우지 않는다. 모르는 값·불명확한 요구사항·존재를 확인 못 한 API가 나오면 멈추고 묻는다.

## C. 버그 규칙
- 바로 고치려 들지 않는다. 먼저 인터뷰로 **재현 조건·최근 변경·관측된 증상**을 좁힌다.
- 가설을 세우고, **가능성 높은 것부터 하나씩** 검증하는 계획을 제시한다.

## D. 회고 규칙
- 내가 "끝났다"고 하면 인터뷰로 **무엇이 잘 됐고 어디서 헛다리를 짚었는지** 정리한다.
- 그 교훈을 다음에 반복하지 않도록 **이 CLAUDE.md에 규칙으로 추가**한다.

## E. 코드 취향
- **파일/모듈 구조:** 얕은 패키지. `src/barum/` 아래 역할별 모듈(`crawl/`, `preprocess/`, `judge/`, `reference/`), 실행은 `scripts/` 또는 `python -m`. 모델(VLM) 호출은 provider를 바꿔 낄 수 있는 **얇은 어댑터**로 분리한다.
- **네이밍:** 식별자는 영어 `snake_case`. 도메인 용어도 **영어로 번역**한다. 로마자 한글 식별자 금지(`gwadae_gwanggo` X → `exaggerated_ad`, `violation`, `review_certified`). 한국어는 사용자 대면 출력·리포트·문서·주석에만.
- **에러 처리:** 계층을 구분한다.
  - *예상된 실패*(빈 상세페이지·429·타임아웃 등) → 상품/이미지 단위로 스킵하고 `status`에 기록, 배치는 끝까지 진행.
  - *예상 못 한 실패*(파싱 깨짐·스키마 위반 등) → 삼키지 말고 즉시 예외로 터뜨린다.
  - *재시도:* 비용 낮은 작업은 1~2회 재시도 후 실패 시 `print`로 알리고 스킵. **VLM 등 과금 호출은 재시도하지 않고** 실패로 기록·스킵하고 실패분만 나중에 재실행한다.
  - 로깅은 `print`.
- **테스트:** 순수 로직(타일 절단 위치·해시 dedup·스키마 파싱·레퍼런스 매칭)만 `pytest` 유닛테스트. 크롤·VLM 등 외부 의존은 목킹하지 않고 **수동 스모크**로 확인.
- **주석:** 중간 밀도. 함수엔 짧은 **한국어 docstring**(역할·입출력), 비자명한 도메인 로직엔 "왜(why)" 주석. 무엇(what)을 그대로 옮기는 주석은 지양.
- **글쓰기(모든 산출물):** em-dash(—) 쓰지 않는다. 삽입구는 쉼표, 괄호, 줄바꿈으로 풀고 문장은 한국어답게 짧게 나눈다. 채팅 답변, 문서(md), 코드 주석, 커밋 메시지에 모두 적용한다.

## F. 디자인·색 규칙 (프론트/목업, 반드시)
- **디자인 규칙은 구속력 있다.** `design/mockups/DESIGN.md` §2(스킬 적재적소)·§3(매번 돌리는 검증, 대비·CVD 절차 포함)는 참고가 아니라 지켜야 할 규칙이다. 목업/UI 작업 전 이 두 절을 먼저 확인한다.
- **차트·KPI/stat 타일·스파크라인·색을 만들거나 바꾸기 전 → `dataviz` 스킬을 먼저 켠다.** 발동 조건이면 예외 없이. 만들고 나서 검증하지 말고, 만들기 전에 스킬을 읽는다.
- **색은 눈으로 판단 금지. 검증기(dataviz `validate_palette.js`)를 돌려 통과한 값만 쓴다.** 심각도/상태 색을 새로 넣거나 인접시킬 때 특히. (DESIGN.md §3.2)
- **앰버(`--warn`)를 심각도 색으로 쓰지 말 것.** 빨강(`--crit`)과 CVD·정상시력 모두 구분 난망(deutan ΔE 2.1 / normal ΔE 10.4, 검증 완료). 시스템은 "빨강만 경보, 나머지 회색"이다.
- **색은 '지금 급한 것'에만.** 비긴급 상태(진행 중·예정 등)에 색을 칠하지 않는다. 상태는 색이 아니라 글자·모양으로.
- **내 맘대로 결정 금지 (A 규칙 재확인).** 새 색·새 상태색·새 컴포넌트·문서화된 규칙에서 벗어나는 예외가 필요하면, 정하지 말고 **멈춰서 선택지를 제시하고 승인을 받는다.**

## G. 공유 워크트리 협업 규칙 (반드시, 사고 재발 방지)
> barum은 PM·디자이너·백엔드 등 **여러 Claude 세션이 같은 디렉터리(`/Users/hani/Desktop/project/barum`)를 동시에 공유**한다. 각자 별도 clone이 아니다. AGENTS.md의 일반 git 흐름(`git checkout main` 등)은 이 특수성을 안 담고 있으니, 아래를 우선한다.
- **메인 워크트리에서 `git checkout <다른 브랜치>`로 HEAD를 옮기지 않는다.** 다른 세션이 작업 중인 파일이 그 순간 통째로 바뀐다. 실제 사고 있었음(2026-08-12, 낡은 로컬 브랜치로 체크아웃해 HEAD가 몇 달치 뒤로 튐, 복구는 됐으나 위험).
- **브랜치 작업은 반드시 `git worktree add <임시경로> -b <브랜치> origin/main`으로 격리**해서 하고, 끝나면 `git worktree remove`로 정리한다. 메인 워크트리 HEAD는 항상 그대로 둔다.
- **커밋은 `git add <내 파일 경로 지정>`만.** `git add .`·`-A` 금지 — 다른 세션이 스테이징해둔 파일이 내 커밋에 딸려 들어간다. 남의 파일(`design/mockups/*`·`docs/handoffs/*`·`docs/standup/*` 등)은 절대 스테이징하지 않는다.
- **PM·팀원 세션은 자주 교체된다(PM 1→2→3→4처럼).** 지시받기 전에 그 세션이 진짜 "현재" 세션인지 확인한다: `list_sessions`로 최근 활동시각·제목의 "(현)" 표시를 보되, 그것만으로 확정하지 말고 가능하면 별도 근거(인수인계 브랜치 존재 등)로 교차검증한다. 제목이 "(현)"이어도 늦게 갱신된 구세션일 수 있다.
- **팀원 실명은 코드네임으로.** 커밋 메시지·파일명·문서 본문에 실명을 쓰지 않는다(매핑은 memory 참조, 확실치 않으면 역할명으로).
- **origin에서 브랜치가 갑자기 여러 개 사라져도 사고가 아닐 수 있다.** 이 저장소는 "PR 머지 시 head 브랜치 자동삭제" 설정이라, 하니가 GitHub 웹에서 연달아 머지하면 그때마다 브랜치가 자동으로 지워진다(2026-08-12 실제로 오탐 있었음). 놀라서 재push하기 전에 `git merge-base --is-ancestor <커밋SHA> origin/main`로 그 커밋이 이미 main에 들어갔는지부터 확인한다. 이미 들어가 있으면 유실이 아니라 정상 동작이니 그냥 둔다.
