# ROADMAP: barum 실행 로드맵 (v1.5)

> 성격: 가변 상태 문서(진행·할 일·담당·일정). 확정 결정은 `PROJECT.md`, 작업 규칙은 `CLAUDE.md`.
> 갱신일: 2026-08-10. 발표일: 2026-08-27(수). 기준 기획서: `2조_최종프로젝트_기획서_v1.5.docx`.
> 방향: 화장품 광고 컴플라이언스 자가검증(브랜드용). 국내 1단계 + 미국 프리플라이트(조건부 2단계). LLM+RAG.

---

## 0. 30초 요약

- 광고(이미지/글)를 넣으면 화장품법 위반 위험을 문구별로 짚어 주고, 무슨 조항 위반인지 근거까지 보여 주는 서비스.
- 화면 3개(홈·새 검사·리포트) + 뒷단 판정 프로그램 + 규칙집(법령·금지표현·성분표).
- (v1.5 추가·팀 합의) 출시 전 상세페이지 뼈대 생성 기능. 효능 문구는 만들지 않고 안전한 초안 + 조건표 + 근거 표기 틀만 제공. 상세는 기획서 v1.5(§6·FR-11). 주차 편성은 팀 논의.
- 발표까지 약 2주 반. 같이 하는 날 주 3~4일. 아래 주차별 계획대로 간다.
- 판정 AI는 Gemini(무료 키). 개발은 Antigravity·Claude Code. 돈은 사실상 0.

---

## 1. 로드맵 (주차별, 쉬운 말)

### 두 가지 원칙
1. 제일 오래 걸리는 "규칙집"부터 시작한다. 디자인 안 끝나도 바로 착수 가능.
2. "넣는 것 / 나오는 것" 형식을 먼저 약속한다. 그래야 화면 팀과 판정 팀이 서로 안 기다리고 따로 일한다.

### 용어 (쉬운 말)
- 규칙집 = 화장품법 위반 조항·금지 표현·기능성 성분표를 AI가 찾아볼 수 있게 정리한 자료 묶음.
- 판정 프로그램(뒷단) = 광고를 넣으면 "이 문구가 위반"이라고 가려내는 프로그램.
- 화면(앞단) = 사람이 보는 웹페이지 3개(홈 / 새 검사 / 결과 리포트).
- 평가셋 = AI가 잘 맞히나 채점하려고 사람이 미리 정답을 매긴 광고 문장 43개. (`backend/data/cosmetic_eval_labeling.xlsx`)

### 이번 주말 (8/9~10) · 가볍게 각자
- 하니: 이 로드맵 팀 공유, 다들 Antigravity·Claude Code 설치 확인.

### 1주차 (8/11~15) · 디자인 확정 + 규칙집 시작
- [같이·하루] 하니+정빈: 화면 3개 디자인 v1.4로 수정 확정. 특히 결과 리포트 화면(문구에 빨간 밑줄 + 옆에 "무슨 법 몇 조 위반" + 위험 정도). 원본 링크 크게, 스크린샷 보존 표시.
- [같이·반나절] 셋이: "넣는 것 / 나오는 것" 형식 한 장으로 약속. (넣는 것 = 광고 이미지/글 + 나라 / 나오는 것 = 문구마다 [어디가·무슨 위반·근거 조항·위험도])
- [따로] 하니+대수: 규칙집 시작. 화장품법 1~4호 조항 + 금지 표현 + 기능성 성분표(미백·주름). 다 공개 자료.
- [따로] 대수 혼자: 평가셋 라벨링(43문장 위반/합법/유형 정답 달기). 시트 이미 준비됨.

### 2주차 (8/18~22) · 본격 만들기
- [따로] 하니+대수: 판정 프로그램. 광고 넣으면 → (긴 이미지 조각내 글자 읽고) → 규칙집 참고해 위반 가려내고 → 리포트로 내보내기. 식품 때 만든 걸 재활용해 화장품용으로 바꾼다.
- [따로] 하니: 화면 뼈대(빈 골격) 먼저 잡아 정빈에게 넘김.
- [따로] 정빈: 화면 3개 코딩. 판정 결과 없으면 가짜 샘플로 모양부터 완성.
- [같이·후반] 화면 ↔ 판정 프로그램 처음 연결.

### 3주차 (8/25~27) · 합치고 발표
- [같이] 진짜 광고 넣으면 리포트까지 한 번에 나오게 마무리.
- [같이] 채점: 판정 프로그램을 평가셋에 돌려 "몇 % 맞히나" 경향 확인.
- [같이] 발표 시연 1개 확실히(미백 크림 이미지 업로드 → 위반 뜸 → 문구 수정 → 재검사하니 사라짐) + 2분 녹화 백업.
- 여유되면 미국 선크림 1건 시연.

---

## 2. 담당

| 일 | 담당 | 비고 |
|---|---|---|
| 디자인(화면 3개) | 하니 + 정빈 | 1주차 초 |
| 규칙집 + 판정 프로그램 | 하니 + 대수 | 크리티컬 패스 |
| 화면 코딩 | 하니(뼈대) → 정빈(세부) | 2주차 |
| 평가셋 라벨링 | 대수 혼자 | 조율시간 아끼려 1인 |
| 통합 + 발표 | 다 같이 | 3주차 |

- 하니가 제일 몰림(리드+개발). 1주차는 규칙집에 집중, 화면 뼈대는 2주차로.

---

## 3. 돈 / AI (비용 사실상 0)

- 판정 AI = Gemini. 유료 크레딧 대신 무료 키(하루 무료 할당량, 분당 횟수 제한 있지만 데모엔 충분)를 쓴다. 코드는 그대로, 키만 무료로.
- 큰 배치 재실행 금지(식품 때 이미지 132개 돌린 게 돈 나간 것). 데모는 광고 몇 개 + 문장 43개뿐이라 원래 몇 십 원 수준.
- 프롬프트 실험은 무료 웹(AI Studio, ChatGPT 무료)에서. API 안 긁는다.
- OpenAI 무료 데이터공유 티어는 대안(데모 데이터는 공개 광고라 공유 무방). 단 코드 새로 붙이는 시간 들어 우선순위 낮음. 비교 실험용으로만.

---

## 4. 도구

- Antigravity / Claude Code = 코드 짜는 도우미(우리가 프로그램 만들 때 쓰는 손).
- Gemini = 실제로 광고를 판정하는 AI(프로그램 안의 머리).
- 도구는 코드 짜는 데, Gemini는 광고 판정하는 데. 헷갈리지 말 것.

---

## 5. 리스크 (제일 조심할 것)

1. 규칙집이 크리티컬 패스. 1주차에 꼭 시작. 판정 클래스(1~4호) 관련 조항부터.
2. 평가셋 정답을 2주차 안에 끝내야 발표에서 "몇 % 맞혀요"를 말할 수 있다.
3. 미국은 마지막에 선크림 1건만. 국내부터 확실히.
4. 통합(화면↔판정)은 처음에 잘 안 붙는다. 2주차 후반에 미리 한 번 붙여 본다.

---

## 6. 데이터 현황 (2026-08-09)

| 자료 | 규모 | 위치 |
|---|---|---|
| 화장품 상세(프로브) | 상품 8개, 이미지 142장 | `backend/11st_probe_cosmetic/` (gitignore) |
| 화장품 평가셋 | 43문장(라벨링 대기) | `backend/data/cosmetic_eval_labeling.xlsx` |
| 이미지 판독 실측 | 14장, 사람 정답 + LLM 판독 | `backend/11st_probe_cosmetic/read_test/` |
| (식품, 재활용) 11번가 상세 | 상품 279개, 타일 274개 | `backend/11st_output/` |
| (식품, 재활용) goldset/holdout | 215 / 331문장 | `backend/data/` |

- 화장품 레퍼런스 팩(규칙집)·고시원료표·온톨로지는 아직 없음(그린필드, 1주차 착수). 1차 출처는 확정됨(→ `reference/cosmetic_sources.md`: 법·별표5·실증고시·기능성고시·관리지침 5종).
- 관련 판단 문서: `docs/화장품_확장_근거.md`, `docs/기술선택_비용성능_분석.md`, `docs/멘토링_피드백_정리.md`.

---

## 7. 기술 참고 (재활용 파이프라인 + 실행 명령어)

식품기에 만든 수집·전처리·판독 코드를 화장품용으로 재활용한다. 판정 프롬프트와 레퍼런스만 화장품으로 교체.

| 파일(backend/) | 역할 | 상태 |
|---|---|---|
| `collect_11st_details.py` | 11번가 상세 수집(중복제거 내장). 화장품 프로브도 이걸로 수집함 | 재활용 |
| `tile_split.py` | 긴 상세 이미지를 조각으로 절단 | 재활용 |
| `src/barum/vlm.py` | Gemini 어댑터(provider 교체 가능) | 재활용 |
| `src/barum/preprocess/ocr.py` | 이미지→문장 판독(현재 Gemini 사용) | 재활용 |
| `src/barum/judge/prescreen.py` | 문장 keep + 유형 hint | 재활용 |
| `schema.sql` | Supabase 7테이블 | 적용 대기 |
| `reference/` | 식품 레퍼런스(방법론 참고). 화장품용 신규 구축 필요 | 참고 |

### 실행 환경 & 명령어
- Python 3.11.9, venv = `backend/venv/`.
- 키: `backend/.env`에 `GOOGLE_API_KEY`(Gemini, 무료 키 권장), `ELEVENTH_ST_API_KEY`(11번가). 하드코딩 금지.
- 스크립트는 `backend/`에서 실행.

```bash
cd backend

# 화장품 상세 수집 (소량, 무료)
./venv/bin/python collect_11st_details.py "기능성화장품" --max-products 8 --delay 1.0 --output-dir 11st_probe_cosmetic

# 타일 분할
./venv/bin/python tile_split.py 11st_probe_cosmetic/details --recursive

# 판독(OCR) → prescreen
./venv/bin/python scripts/run_ocr.py
./venv/bin/python scripts/run_prescreen.py

# 테스트
./venv/bin/python -m pytest -q
```

### 11번가 상세 취득 경로
```
https://www.11st.co.kr/products/{상품번호}/view-desc
```
- plain HTML, `<img src>`에 상세 이미지 직접. 안티봇 없음. 연구목적 소량(crawl-delay ≥ 1s) 수집.
