# 레퍼런스팩·온톨로지·판정기준 확장 (2026-08-12, DB 2)

> 작업 착수 시점(9cd42c4, 2026-08-12 새벽)과 완료 시점을 git 커밋 기준으로 비교했다. 수치는 전부 git 명령으로 직접 센 것이지 기억에 의존한 소급 추정이 아니다.
> 관련 커밋: `feature/be-cases-expansion`(f88d05a, 7696232), `docs/be-ontology-opencrab-pointer`(5d0e861, a3a75c1), `feature/be-approved-efficacy-phrases`(f04360e~fa58857).

## 1. 실사례 (cases.md → Supabase reference_cases)

| | 이전 | 이후 |
|---|---|---|
| cases.md §1 표 건수 | 9건 (행정처분 확정 사례만) | 52건 |
| Supabase `reference_cases` 임베딩 | 9건 | 52건 (`load_cases.py` 재적재, text-embedding-3-small) |
| 위반유형 커버리지 | T1/T2/T5/T6 일부 | T1/T2/T5/T6 전부, "판정불명확" 표기로 불확실한 라벨 구분 |
| 출처 문서 | 식약처 행정처분 현황(2024.11.15 코스인코리아 정리) 1건 | + 식약처 지침·정책발표자료 PDF 5종(표시광고가이드라인·정책설명회·인체적용시험결과·2026지침·해설서) |

**어떻게 늘었나**: 하니가 전달한 화장품 표시광고 위반사례·지침 PDF 8개 중 5개(문서2·3·5·7·8)에서 실제 위반 광고 문구를 원문 그대로 추출, 문서 간 중복(예: "Herbal Face Food" 사례가 3개 문서에 반복)은 병합해 43건으로 정리 후 기존 9건에 추가. 처분 이력 없는 지침 예시는 처분란에 "없음"으로 명시해 실제 행정처분 사례와 구분.

## 2. 판정기준 (prohibited_expressions.md/json)

| | 이전 | 이후 |
|---|---|---|
| §1 표 행 수 (.md) | 15행 | 17행 |
| rows 수 (.json) | 15행 | 17행 |

**추가된 것**: "특허 표현을 통한 우회적 의약품효능 암시"(T1, 신규 패턴), "모공수축/축소 표방"(T5, 하니 2026-08-12 확정: 실증자료 없으면 검토필요·있으면 예외). §4에 "광고 제목명도 판단대상"(2025.1.21 개정)·"추출물 함량 표시 기준"(2025.8.14 개정) 주의사항도 추가.

**교차검증 결과**: PDF에서 뽑은 신규 후보 124건 중 약 110건은 기존 §1과 이미 겹쳤다(기존 팩이 탄탄했다는 뜻). judge_rules.json은 "규정 리서치로 검증된 것만" 원칙이라 이번엔 손대지 않고 후보만 남김.

## 3. 온톨로지 (OpenCrab)

| | 이전 | 이후 |
|---|---|---|
| barum 관련 팩 수 | 0 | 5 |
| 다루는 개체 | 없음 | ViolationType·RuleTag(T코드)·Expression·Ingredient·Case·Statute 6종 |
| Case(적발사례) 커버리지 | 없음 | cases.md 예시 4건(기본팩) + 지침·정책발표자료 43건(확장팩) |

**어떻게 늘었나**: 기존 레퍼런스 문서(prohibited_expressions.md·functional_ingredients.md·cases.md·violation_types/*)를 온톨로지 그래프로 변환해 4개 팩으로 최초 구축. T5/T6 분리·검토필요≠미판정·일반수식어 미확정 등 함정 3가지를 본문에 명시해 반영. 이후 실사례 43건 확장을 5번째 팩으로 동기화. 저장은 이 저장소가 아니라 OpenCrab 쪽이라 `reference/cosmetic_kr/ontology_opencrab.md`가 포인터 역할.

## 4. 기능성화장품 인정문구 (신규 파일, FR-11 create 모드용)

| | 이전 | 이후 |
|---|---|---|
| `approved_efficacy_statements.md/json` | 파일 없음 | 미백·주름개선·자외선차단 3종, 전부 confirmed |
| create 모드 인정문구 생성 | 데이터 없어 스킵(베베 확인) | 심사규정 별표4 원문(하니 확보) 기준 정상 노출 |

**과정**: 처음엔 easylaw.go.kr(법제처)+해설서 교차검증한 초안(문구가 별표4 원문보다 장문)으로 시작했으나, 하니가 별표4 고시 원문을 직접 확보해서 대조한 결과 실제 문구는 훨씬 짧았다(예: 미백 "피부의 미백에 도움을 준다."). 자외선차단은 원문에 별도 "효능·효과" 문장이 없어 섹션 제목을 다듬는 방식으로 하니가 결정. 스키마를 최상위 status에서 카테고리별 status로 바꿔서 확정된 카테고리부터 순차적으로 살아나게 설계(베베 게이트 코드 PR #56과 짝).

## 요약
| 항목 | 이전 | 이후 |
|---|---|---|
| 실사례 (cases.md/DB) | 9건 | 52건 |
| 판정기준 행 | 15행 | 17행 |
| 온톨로지 팩 | 0개 | 5개 |
| 인정문구 카테고리 | 0개 | 3개(confirmed) |
