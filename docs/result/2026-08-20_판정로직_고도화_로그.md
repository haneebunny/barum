# 판정로직 고도화 로그 (2대 도도)

이번 라운드부터, 끝나고 몰아서 정리하지 않고 **작업이 끝나는 시점마다 바로 기록**한다
(2026-08-20, 하니 지시). 메모리(`~/.claude/projects/.../memory/`)엔 세션 간 인계용으로 요약을
남기고, 여기엔 사람이 읽을 근거·수치·판단을 남긴다. 최신 항목이 아래로 쌓인다.

이전 라운드 기록: [2026-08-19_판정로직_고도화_1라운드_지표.md](2026-08-19_판정로직_고도화_1라운드_지표.md),
[2026-08-19_에이전틱_증빙검증_설계.md](2026-08-19_에이전틱_증빙검증_설계.md)

---

## 2026-08-20 ① 정답셋 재스윕 (베이스라인 확인)

963문장 재스윕. 지난 라운드 종료 시점 값과 완전 일치 — 최근 머지분(#191~196)이 규칙집·
`rules.py`를 안 건드려서 드리프트 없음.

- 규칙 단독 위반탐지: 8/16
- 규칙 오탐: 0건
- 증발(VLM에도 안 감): 7건

## 2026-08-20 ② "증발"이 뭔지 정리 (팀장 질문 답변)

규칙이 문장을 legal_allow(합법 확정) 또는 out_of_scope(대상외)로 확정하면 VLM 호출 자체를
스킵한다([cosmetic.py:504-538](../../backend/src/barum/judge/cosmetic.py)). 정답이 위반/검토필요인
문장이 이 경로로 빠지면 tp도 fp도 아닌 채로 그냥 사라진다 — 이게 "증발"이다. 규칙 미매칭으로
VLM에 정상 위임되는 것과는 다르다(그건 증발이 아니라 정상 흐름).

## 2026-08-20 ③ legal_allow 5호 마커 누락 발견 + 수정 (PR #197, 머지됨)

**원인**: `탄력`/`민감`/`예민`은 "단독이면 합법"인데, 2026-08-19에 "2호(미백 등) 클레임이 같이
있으면 예외 취소" 안전장치(`context_exceptions.unsafe_markers`)를 붙였다. 이 목록이 2호 키워드만
챙기고 5호(저자극·시험완료류)는 안 챙겨서, 증발 7건 중 4건이 여기 걸렸다.

**수정**: `unsafe_markers`에 `"저자극"`·`"완료"` 추가.

기존에 "저자극"을 독립 플랫 키워드로 안 쓰기로 한 결정(`저자극 딥클렌징`·`저자극 토너` 2건
합법과 충돌해서 기각)과는 다른 메커니즘이라 재충돌 안 함 — `unsafe_marker`는 탄력/민감/예민이
**이미 매치된 문장에서만** 작동하는 취소 스위치라, 그 두 문장(탄력/민감/예민 없음)엔 영향 없음.

**검증**:
- `rule_sweep.py`: 증발 7건 → 3건, 규칙 오탐 0건 유지, tp/fp 불변(둘 다 `none()`으로 빠져
  VLM 경로로 정상 위임 — 기존 정책 그대로).
- 기존 테스트 2건(`test_sensitive_alone_is_still_legal_allow`, `test_uv_abbreviation_is_not_a_marker`)이
  필러 텍스트에 "완료"·"저자극"을 우연히 포함해 깨짐 → 픽스처 수정 + 회귀테스트 2건 추가.
- pytest 462 passed.

**부수효과**: ESCO 증빙 사례 2건("민감성 사용적합 임상을 완료한 제품")도 legal_allow free-pass를
잃고 VLM(+ PR#194 증빙대조 경로)으로 넘어감. 비용 소폭 증가, 정확도 손실 아님.

**남은 증발 3건**: 텍스트 마커가 없는 케이스라 이 방법으론 안 풀림 — ④에서 하나 더 처리.

PR: https://github.com/haneebunny/barum/pull/197 (머지 완료)

## 2026-08-20 ④ 잔여 증발 3건 분류 + 비사보롤 마커 추가 (PR #198)

③ 이후 남은 증발 3건을 하나씩 근거 확인:

| 문장 | 정답 | 처리 |
|---|---|---|
| "민감한 피부도 걱정 없는 **알파 비사보롤**" | 검토필요 | **처리함** — 아래 참고 |
| "탄력·광채 시너지 콜라보"(바쿠치올×이데베논) | 검토필요 | **보류** — 근거 없음 |
| "늘어진 탄력과 넓어진 모공이 고민이신 분" | 애매 | **보류** — 정답 자체가 불확실 |

**처리한 것 — 알파-비사보롤**: `reference/cosmetic_kr/functional_ingredients.md` 8행에
"알파-비사보롤 0.5%"가 미백 기능성 고시원료로 등재돼 있다. "미백"이라는 단어 없이 성분명만으로
암묵적 2호 클레임을 거는 패턴인데, "민감"의 legal_allow에 먼저 걸려 증발했다. `unsafe_markers`에
`"비사보롤"` 추가로 해결.

**보류한 것 — 바쿠치올·이데베논**: `reference/cosmetic_kr/` 전체를 검색했으나 두 성분 모두
근거를 못 찾았다. "근거 없이 마커 추가 금지" 원칙상 지금은 손 안 댐. 이 성분들이 실증대상인지
아닌지는 팩 리서치가 별도로 필요하다 — 팀장 판단 필요 사안으로 남겨둠(착수 안 함).

**보류한 것 — "늘어진 탄력과 넓어진 모공"**: 정답셋 라벨 자체가 "애매"라 규칙으로 확정할 근거가
없다. VLM 판단에 맡기는 게 맞다고 보고 규칙 변경 안 함.

**검증**:
- `rule_sweep.py`: 증발 3건 → 2건, 규칙 오탐 0건 유지, 판정 바뀐 문장 1건(위 표의 비사보롤
  문장, `legal_allow(민감) -> none()`).
- 회귀테스트 1건 추가(`test_sensitive_with_functional_ingredient_name_is_not_legal_allow`).
- pytest 469 passed.

PR: https://github.com/haneebunny/barum/pull/198 (아래 표에 갱신)

## 진행 상황 요약 (이 문서 갱신 시점 기준)

| 시점 | 증발 | 규칙 단독 탐지 | 규칙 오탐 |
|---|---|---|---|
| 라운드 시작(①) | 7 | 8/16 | 0 |
| ③ 이후 | 3 | 8/16 | 0 |
| ④ 이후 | **2** | 8/16 | 0 |

남은 2건은 각각 다른 이유(근거 부재 1건, 라벨 불확실 1건)로 규칙 레벨에서 더 줄일 게 없는
상태다. 다음 후보는 규칙 밖(VLM 프롬프트·품질) 쪽에서 찾아야 한다.

---

# 규칙 밖(프롬프트·컨텍스트·RAG) 라운드

## 2026-08-20 ⑤ 배포 파이프라인 재평가 (실호출)

`scripts/eval_ragjudge.py`로 40문장 골드셋에 RagJudge(gpt-5-mini, 규칙+grounding+pgvector
사례검색)를 실제로 돌렸다.

- 전체 일치율 25/40 = 62.5%
- 미탐(위반→미플래그) 1건
- 오탐(합법→위반유형 finding) 12건 (위반 1 + 검토필요 11)

**미탐 1건은 고칠 수 있는 결함이 아니다.** `#14 "진짜예요!!"` — 바로 앞 문장(`#13 "효과로
증명합니다."`)에 이어지는 감탄사다. 문장 자체엔 효능어도 주장도 없어서 문맥 없이 이 문장만
보면 어떤 모델도 못 잡는다. 평가 방식(문장 독립 채점)의 한계지 프롬프트 문제가 아니다.

## 2026-08-20 ⑥ **측정 도구가 낡았다** (⑤의 "오탐 12건"은 대부분 허수)

⑤의 오탐을 하나씩 보다가 이상해서, 40문장 골드셋(`data/cosmetic_eval_labeling.xlsx`)의 라벨을
현행 963문장 정답셋(`label_worksheet_combined.xlsx`)과 **문장 단위로 대조**했다.

**두 셋에 같은 문장이 35건 겹치는데, 그 중 14건(40%)이 합법 여부부터 서로 다르다.**

| # | 40문장 셋 | 963문장 셋 | 문장 |
|---|---|---|---|
| 1 | 합법 | 검토필요 | 개기름 OFF 보습 ON |
| 4 | 합법 | 검토필요 | 피지컨트롤! 번들거리는 개기름 CUT! |
| 6 | 애매 | 합법 | 미네랄로 인한 피부 보습효과 |
| 7 | 합법 | 검토필요 | 맑음 수분 공급 및 피부 진정 작용에 도움 |
| 20 | 합법 | 검토필요 | 콜라겐 추출물 1000ppm 함유 |
| 21 | 합법 | 검토필요 | 겔타입의 완벽한 수분 보습막 |
| 22 | 합법 | 검토필요 | 최적의 유수분 밸런스 |
| 24 | 합법 | 검토필요 | 콜라겐, 히알루론산 함유로 파워 수분 공급 |
| 25 | 합법 | 검토필요 | 피부 진정에 탁월한 7가지 한방 추출물 함유 |
| 29 | 2호_기능성오인 | **합법** | 피부의 미백에 도움을 준다. |
| 30 | 2호_기능성오인 | **합법** | 피부의 주름개선에 도움을 준다. |
| 34 | 합법 | 검토필요 | 저자극 테스트 |
| 35 | 합법 | 애매 | (PDRN 저함유 비방 문장) |
| 37 | 합법 | **위반** | 어둡고 칙칙한 피부톤 개선 및 미백효과를 원하시는 분 |

40문장 셋은 검토필요 정책(PR #175) 이전에 만들어진 낡은 라벨이다. ⑤의 "오탐 12건" 중 최소
7건(#4·#7·#21·#22·#24·#25·#37)은 **AI가 현행 정답셋과 일치하는 판정을 냈는데 낡은 라벨
때문에 오탐으로 집계된 것**이다. #37은 오히려 현행 정답셋이 "위반"인데 AI가 "검토필요"로
약하게 부른 것이라 오탐이 아니라 과소평가다.

**결론: `eval_ragjudge.py`·`score_eval.py`의 40문장 지표는 지금 신뢰할 수 없다.** 이 지표를
기준으로 프롬프트·RAG A/B를 돌리면 낡은 라벨 쪽으로 최적화하게 된다. 프롬프트·컨텍스트
고도화에 착수하기 전에 측정 도구부터 현행 정답셋 기준으로 맞춰야 한다.
(교훈은 이미 있었다 — "재측정 전 최신 정답셋 필수". 이번엔 도구가 낡은 셋을 하드코딩하고
있어서 그 규율이 우회됐다.)

## 2026-08-20 ⑦ 진짜 오탐 발견 — 식약처 **인정문구**를 위반으로 찍는다

⑥의 대조에서 `#29`·`#30`이 눈에 띄었다. 40셋은 "2호 위반", 963셋은 "합법"인데, 이 문장들은
우리 레퍼런스팩 `approved_efficacy_statements.md`에 **식약처가 인정한 정형 효능효과 문구**로
등재된 바로 그 문장이다(고시 제2023-61호 별표4, 팀장이 원문 대조 확정).

실호출로 확인(3회 반복, **편차 없이 3/3 동일**):

| 문장 | 판정 결과 |
|---|---|
| "피부의 미백에 도움을 준다." | 2호_기능성오인 / 검토필요 |
| "피부의 주름개선에 도움을 준다." | 2호_기능성오인 / 검토필요 |
| "피부를 곱게 태워주거나 자외선으로부터 피부를 보호하는 데 도움을 준다." | 2호_기능성오인 / 검토필요 |

세 문장 모두 `approved_efficacy_statements.md` §1~§3에 인정문구로 실려 있는 원문 그대로다.
**법이 "이렇게 쓰라"고 정해준 문구를 우리 시스템이 의심스럽다고 찍고 있다.**

**원인 — grounding 컨텍스트가 금지 문서로만 채워져 있다.** `reference/context.py`의
`_REGULATION_FILES`를 확인했다:

```
prohibited_expressions.md              (금지표현)
violation_types/type_1_...             (위반 판정기준)
violation_types/type_2_...             (위반 판정기준)
violation_types/type_5_...             (위반 판정기준)
functional_ingredients.md              (성분 기준표)
+ cases.md                             (적발 사례)
```

**전부 "무엇이 위반인가" 문서다. "무엇이 합법인가"를 알려주는 문서가 하나도 없다.**
`approved_efficacy_statements.md`(3,691자)는 레퍼런스팩에 있는데 컨텍스트에 안 실린다.
그 파일엔 "판정에 쓰는 법" 절까지 적혀 있어서 원래 판정에 쓰라고 만든 문서인데도 그렇다.

`JUDGE_PROMPT`도 같은 방향이다 — "2호_기능성오인: 미백·주름개선·자외선차단 기능성 효능을
주장", 확정도 기준에 "기능성 표방이라 심사·보고 여부를 확인해야 한다"까지 있고, 인정문구
예외는 어디에도 없다. 모델은 금지 근거만 보고 판단하니 계통적으로 플래그 쪽으로 쏠린다.

이건 규칙 레벨에서 안 보이던 종류의 결함이다(규칙집은 애초에 미백/주름을 2호로 안 넣고 VLM에
맡기는 설계라 스윕에 안 잡힌다).

## 2026-08-20 ⑧ 열린 결정 (착수 전 승인 필요)

⑦의 수정 방향에 판정 정책 판단이 걸려 있다. 인정문구를 **그대로** 쓴 광고를 어떻게 볼 것인가?

- **합법으로 본다** — 963 정답셋이 이 입장이다(#29·#30을 합법으로 라벨). 법이 정해준 문구를
  그대로 쓴 것이니 그 자체로는 문제 삼지 않는다.
- **검토필요를 유지한다** — 지금 시스템 입장이다. 인정문구를 쓸 자격(기능성 심사·보고 등록)이
  있는 제품인지는 우리 입력으로 알 수 없다. `_functional_evidence`도 같은 논리로 "이름+함량이
  다 맞아도 합법까지는 못 간다"고 못박고 있다.

둘 다 근거가 있어서 도도가 혼자 정할 사안이 아니다(레퍼런스팩·정답셋 라벨이 걸린 판단).

**→ 팀장 결정(2026-08-20): 전성분 여부로 갈라서 처리.** 전성분이 입력되고 고시원료·기준함량을
충족하면 합법, 확인이 안 되면 검토필요. 기존 `_functional_evidence` 로직의 마지막 분기
("기준 충족이어도 등록 여부 불명이라 검토필요 유지")를 합법으로 바꾸는 것에 해당한다.
착수 전에 측정 도구부터 고친다(⑨) — ⑥에서 확인했듯 지금 지표로는 개선 여부를 못 가린다.

## 2026-08-20 ⑨ 평가 골드셋 ver2 (측정 도구 교체)

⑥의 결론대로, 프롬프트·RAG를 손대기 전에 평가셋부터 현행 기준으로 맞췄다.

**근본 원인은 축이 하나 없다는 것이었다.**

| | ver1 (40문장 골드셋) | 963 정답셋 |
|---|---|---|
| 라벨 축 | **위반유형 1축** | **판정(확정도) + 위반유형 2축** |
| "검토필요" 표현 | **칸 자체가 없음** | 168건 |
| 만든 시점 | 검토필요 정책(#175) 이전 | 현행 |

ver1은 "검토필요"를 적을 칸이 없어서, 검토필요여야 할 문장을 전부 합법 아니면 위반유형 중
하나로 밀어넣어야 했다. **963셋이 틀린 게 아니라 ver1의 표현력이 부족했던 것이다.**

**ver2 생성 방식** (`scripts/build_eval_goldset_v2.py`):
1. ver1 43행을 읽는다.
2. 963셋에 같은 문장이 있으면(**35건**) 그 라벨을 그대로 이식 — 이미 검수된 라벨이라
   사람이 다시 볼 필요가 없다.
3. 963셋에 없는 **8건**만 수동 라벨(팀장 승인). 판단 근거를 스크립트에 그대로 적어 뒀다.
4. `data/cosmetic_eval_labeling_v2.xlsx`로 저장. **ver1은 안 건드린다**(과거 측정치와의
   연속성 보존, 예전 기록 삭제 금지 규칙).

`backend/data/`가 gitignore라 xlsx 자체는 커밋되지 않는다. 그래서 **라벨 결정을 생성
스크립트에 담아** 리뷰·재현이 가능하게 했다(새 환경에선 이 스크립트를 한 번 돌리면 된다).

**ver1 → ver2 라벨 분포 변화**:

| | ver1 | ver2 |
|---|---|---|
| 합법 | 25 | **14** |
| 검토필요 | (표현 불가) | **19** |
| 위반 | (확정도 없음) | 4 |
| 대상외 | 3 | 5 |
| 애매 | 3 | 1 |

ver1에서 합법이던 11건이 ver2에서 검토필요로 이동했다. ⑥에서 "허수 오탐"으로 지목한 것들이다.

**채점기도 2축으로 확장**(`score_eval.py`·`eval_ragjudge.py`):
- 정답이 **검토필요**면 "플래그를 달았는가"만 본다. 963셋이 검토필요 160건 중 154건에
  유형을 안 매기기 때문이다("유형은 정해도 확정은 못 하는" 상태). 유형까지 요구하면
  정답에 없는 걸 요구하는 셈이 된다. AI가 위반으로 더 세게 부른 건 미탐이 아니라 정답.
- 정답이 **위반**이면 유형까지 맞아야 정답(기존과 동일).
- 정답이 **합법·대상외**면 finding이 없어야 정답(기존과 동일).
- **애매는 채점 제외**(1건).
- 검토필요 포착 건수를 별도 지표로 출력한다(위반탐지율과 합산 금지 — 모집단이 다르다).

함정 하나 피했다: `scored = [r for r in rows if r["human"] in LABELS]` 그대로 뒀으면
유형 칸이 빈 검토필요 9건이 **조용히 채점에서 빠졌을 것**이다. 확정도가 있으면 채점 대상이
되도록 필터를 고쳤다(채점대상 42/43, 제외는 애매 1건뿐).

## 2026-08-20 ⑩ ver2로 재측정 — 지표가 62.5% → 81.0%

**⚠ 이건 성능 개선이 아니다. 시스템은 아무것도 안 바뀌었다.** 같은 코드·같은 모델
(gpt-5-mini)·같은 RagJudge에 평가셋만 ver1 → ver2로 갈아끼운 결과다. 차이 18.5%p는
전부 **낡은 라벨이 만들던 측정 오차**였다. 발표·보고에 쓸 때 반드시 이 단서를 붙일 것 —
안 붙이면 "고도화로 성능이 올랐다"로 오독된다.

| 지표 | ver1 (낡은 셋) | ver2 (현행 기준) |
|---|---|---|
| 채점 대상 | 40문장 | 42문장 |
| 전체 일치율 | 25/40 = **62.5%** | 34/42 = **81.0%** |
| 미탐 | 1건 | 3건 |
| 오탐 | **12건** (위반1 + 검토필요11) | **4건** (위반0 + 검토필요4) |
| 검토필요 포착 | (측정 불가, 축 없음) | **16/19건** |

오탐이 12건 → 4건으로 줄어든 게 핵심이다. 사라진 8건은 AI가 틀렸다가 맞은 게 아니라,
**처음부터 현행 정답셋과 일치하는 판정이었는데 ver1의 낡은 라벨이 오탐으로 세던 것**이다.
위반 오탐이 0건이 된 것도 같은 이유다(ver1의 유일한 위반 오탐 #35는 963셋에서 "애매"라
ver2에선 채점 제외).

미탐이 1 → 3으로 "늘어난" 것도 실제 악화가 아니다. ver1엔 검토필요 축이 없어서 애초에
검토필요 미탐을 셀 수 없었다. ver2에서 새로 보이게 된 3건은 전부 검토필요 행이다.

### 남은 오탐 4건 (정답=합법인데 플래그)

| # | 문장 | AI 판정 | 성격 |
|---|---|---|---|
| 29 | 피부의 미백에 도움을 준다. | 2호/검토필요 | **인정문구**(⑦) |
| 30 | 피부의 주름개선에 도움을 준다. | 2호/검토필요 | **인정문구**(⑦) |
| 16 | 눈가 뿐만 아니라 이마, 목, 팔자 주름까지 아낌없이 듬뿍 듬뿍 | 2호/검토필요 | 진짜 오탐 — 용량 소구인데 '주름' 단어에 반응 |
| 36 | 시술 후 예민해진 피부 진정에 도움이 필요하신 분 | 1호/검토필요 | 진짜 오탐 — 대상 고객 서술 |

**오탐 4건 중 2건(50%)이 ⑦에서 찾은 인정문구 건이다.** 승인받은 수정(전성분 기준 분기)이
정확히 이 절반을 겨냥한다.

### 남은 미탐 3건 (정답=검토필요인데 미플래그)

| # | 문장 | 성격 |
|---|---|---|
| 1 | 개기름 OFF 보습 ON | VLM이 안 잡음 |
| 20 | 콜라겐 추출물 1000ppm 함유 | **prescreen이 버림**(판정기가 보지도 못함) |
| 34 | 저자극 테스트 | VLM이 안 잡음 |

`#20`은 1차 필터가 "판정 대상 아님"으로 버려서 판정기까지 가지도 못했다. 이번 실행에서
prescreen이 버린 5건 중 하나다(나머지는 브랜드명·제품명·전성분 등 정당한 제외).
prescreen 누수는 이미 아는 구조적 이슈다.

`#34 "저자극 테스트"`는 규칙 라운드(③)에서 다룬 '저자극'과 같은 표현인데, 규칙은
legal_allow 취소까지만 하고 실제 판정은 VLM에 넘긴다. 규칙은 제 몫을 했고 VLM이 안 잡은
것이라 규칙 레이어 문제가 아니다.

## 2026-08-20 ⑪ 인정문구 수정 구현 (⑦·⑧ 결정 반영)

승인받은 "전성분 여부로 갈라서 처리"를 두 군데에 넣었다.

**(1) grounding에 합법 근거 문서 추가** (`reference/context.py`)
`_REGULATION_FILES`에 `approved_efficacy_statements.md`를 넣었다. ⑦에서 확인했듯
그 전까지 목록이 전부 "무엇이 위반인가" 문서였고 합법 근거가 하나도 없었다.
컨텍스트 길이 실측: **20,270자 → 36,503자**(cases.md 포함 기준). 캐시 적중률이 높아
연속 배치에선 부담이 적지만, 콜드 스타트 비용은 재측정 대상으로 남는다.

**(2) `_functional_evidence` 마지막 분기를 합법으로** (`judge/cosmetic.py`)
"고시원료 있음 + 함량 기준 충족"을 검토필요에서 **합법(finding 없음)**으로 바꿨다.
플래그 `None`이 "합법 확정, finding 만들지 마라"는 신호고 호출부가 `continue`한다.

바꾼 이유: 이전 논리("등록 여부를 모르니 합법까진 못 간다")는 등록 여부가 **우리 입력에
애초에 없는 정보**라, 그 기준이면 완전히 정상적인 기능성화장품 광고도 영원히 검토필요를
못 벗어난다. 판단 기준을 "우리가 확인할 수 있는 것"(전성분·함량)에 맞춘 것이다.

## 2026-08-20 ⑫ **그 수정이 새 미탐 경로를 만들었다** (실측으로 잡아서 막음)

⑪을 구현하고 양방향으로 재봤다(⑦에서 인정문구만 보고 끝냈으면 놓쳤을 것이다).
전성분을 충족시킨 상태에서 **과장 표현이 붙은 문장**을 넣어 봤다.

| 문장 | 결과 (3회 반복) |
|---|---|
| 단 3일만에 완벽하게 미백되는 기적의 크림 | **미플래그(합법) 3/3 — 누수** |

**구조적 원인**: 한 문장에 라벨이 하나뿐이다. VLM이 이 문장을 2호로 분류 → 성분이
맞아서 합법으로 강등 → "완벽하게·기적의·단 3일만에"라는 과장이 **통째로 사라진다**.
원래 설계 주석이 경고하던 "강등이 새 미탐 경로가 된다"가 실제로 일어난 것이다.

**규칙집도 못 막았다.** 5호 needs_review 키워드에 `"완벽한"`이 있는데 문장은 `"완벽하게"`라
활용형이 달라 매칭이 안 됐다(`match_rule` 반환 `None` 확인).

**막은 방법**: `reference/rules.py`에 `has_exaggeration()`을 추가하고(어간 기준:
완벽·최적·탁월·최고·최상·유일·파워·기적), 성분이 맞아도 이게 걸리면 합법으로 안 내리고
검토필요로 둔다. 근거는 `prohibited_expressions.md:85`("완벽한·최적의·파워·탁월한·
최고·최상 등 절대적·과장 표현은 객관적 근거 없으면 검토필요") +
`approved_efficacy_statements.md` 판정에 쓰는 법 4항("인정문구를 벗어난 과장 표현이
붙으면 별개로 T5 판정 가능") — 팩이 이미 "성분 정합과 과장은 따로 본다"고 말하고 있었다.

**가드 적용 후 재검증 (4케이스 × 3회, 편차 없음)**:

| 케이스 | 기대 | 결과 |
|---|---|---|
| 인정문구(미백) + 성분충족 | 합법 | 미플래그 ✓ |
| 인정문구(주름) + 성분충족 | 합법 | 미플래그 ✓ |
| 단 3일만에 완벽하게 미백되는 기적의 크림 | 플래그 유지 | 2호/검토필요 ✓ |
| 최고의 주름개선 효과를 자랑하는 앰플 | 플래그 유지 | 2호/검토필요 ✓ |

팀장 확인: "과장도 잡아야 해서 그게 맞다" — 가드 유지 확정(2026-08-20).

**검증**: pytest **473 passed**(누수 회귀테스트 포함 4건 신규), `rule_sweep` 회귀 없음
(규칙 단독 8/16, 오탐 0건, 증발 2건 — ④ 이후와 동일).

**교훈**: ⑦에서 "인정문구가 플래그된다"만 보고 고쳤으면, 고친 자리에서 반대 방향 구멍이
생긴 걸 몰랐을 것이다. **양방향 측정은 발견할 때만이 아니라 고친 다음에도 해야 한다.**
이번 라운드에서 이 규율이 실제로 결함을 하나 잡았다.

## 2026-08-20 ⑬ 수정 후 재측정 — **개선 주장 불가(노이즈 범위)**

⑪·⑫를 적용하고 ver2로 다시 돌렸다.

| 지표 | ⑩ (수정 전) | ⑬ (수정 후) |
|---|---|---|
| 전체 일치율 | 34/42 = 81.0% | 33/42 = **78.6%** |
| 미탐 | 3건 | 4건 |
| 오탐 | 4건 (위반0+검토4) | 4건 (위반0+검토4) |
| 검토필요 포착 | 16/19 | 15/19 |

**차이는 문장 1건이다. ±6%p 편차 규율상 이건 개선도 악화도 아니다(노이즈).**
1회씩만 재서 "수정 후 2.4%p 떨어졌다"고 쓰면 안 된다. 실제로 늘어난 미탐 1건은
`#14 "진짜예요!!"`인데, ⑤에서 이미 "문맥 없이는 어떤 모델도 못 잡는 문장"으로 분류한
바로 그 행이다. 회차마다 잡혔다 안 잡혔다 하는 전형적인 편차 사례다.
(prescreen이 버린 문장 수도 5건 → 7건으로 흔들렸다. 같은 성격의 편차다.)

**중요 — 이 지표에는 ⑪ 수정의 효과가 아예 안 나타난다.** `eval_ragjudge.py`는
**전성분을 안 넘긴다.** 그래서 `#29`·`#30`(인정문구)은 "전성분 없음 → 검토필요"
경로를 타고, 이는 승인된 정책상 **정확한 동작**이다. 그런데 ver2 라벨은 963셋에서
이식한 "합법"이라 오탐으로 집계된다. 즉 이 2건은 **평가 방법론의 한계지 결함이 아니다**
(2호는 전성분 미입력 시 검토필요가 상한이라는, 이전 라운드에도 기록된 제약).

수정의 실제 효과는 전성분을 넘긴 직접 실측(⑫의 4케이스 × 3회)에서만 확인된다.
평가셋에 상품별 전성분을 붙이는 건 별도 작업이라 다음 라운드 후보로 남긴다.

## 2026-08-20 ⑭ 다음 라운드 후보 (기록만, 착수 안 함)

- **규칙집 5호 키워드가 활용형만 담고 있다.** `"완벽한"`은 있는데 `"완벽하게"`를 못 잡고,
  팩 §85가 명시한 `"탁월한"`은 규칙집에 아예 없다. ⑫에서 `has_exaggeration`으로 우회했지만
  규칙집 자체를 어간 기반으로 정리하는 게 근본 수정이다. `rule_sweep`으로 오탐 영향을
  재고 나서 판단할 것(맨 "최고"가 "최고급 원료" 같은 합법 문구에 걸릴 위험이 있다).
- **prescreen이 검토필요 문장을 버린다.** ⑩의 미탐 3건 중 `#20 "콜라겐 추출물 1000ppm
  함유"`가 1차 필터에서 탈락했다. 이미 아는 구조적 이슈지만 ver2 지표로 재측정 가능해졌다.
- **콜드 스타트 비용 재측정.** ⑪로 컨텍스트가 20,270자 → 36,503자로 늘었다. 연속 배치는
  캐시 적중률이 높아 괜찮지만(이전 실측 99.3%), 사용자가 몇 시간에 한 번 검사하는 운영
  패턴이면 매번 콜드 스타트다. 운영 패턴 확정 후 재측정 필요.
- **평가셋에 전성분을 붙이는 문제.** ⑬에서 드러났듯 `eval_ragjudge.py`가 전성분을 안 넘겨
  2호 관련 수정의 효과가 지표에 안 잡힌다. 골드셋 문장들의 원 상품 전성분을 붙이면
  2호 경로를 제대로 잴 수 있다. 상품코드는 ver2의 "출처(이미지)" 열에 남아 있어
  `11st_details_*.json`에서 끌어올 수 있을 것으로 보인다(미확인).

## 이 라운드 총괄

| | 라운드 시작 | 현재 |
|---|---|---|
| 규칙 증발 | 7건 | **2건** |
| 규칙 오탐 | 0건 | 0건 (유지) |
| 평가 지표 신뢰성 | ver1(축 1개, 40% 라벨 불일치) | **ver2(축 2개, 현행 정답셋 기준)** |
| 인정문구 판정 | 3개 다 위반 플래그 | 전성분 충족 시 합법(과장 있으면 유지) |
| grounding 근거 | 금지 문서만 | 금지 + **합법 근거** |

PR: #197(증발 5호마커) · #198(비사보롤) · #203(로그 신설) · #207(진단) · 이번 PR.
