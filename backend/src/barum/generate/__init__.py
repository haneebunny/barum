"""콘텐츠 생성·개선 (FR-11/13).

개선(improve) 모드: 검사된 원본 광고를 안전 버전으로 만든다.
- 위반 문구는 조건표(remediation)로 결정적 치환(replace.py).
- 저위험 서술은 LLM 생성(content.py, B단계).
- 생성물은 /check로 재검증(C단계).
바깥(API)은 이 패키지의 얇은 함수에만 의존한다.
"""
