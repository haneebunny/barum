# PROVE-IT

이 파일은 prove-it 스킬이 자동으로 쌓는 전후 측정 기록입니다. 최신 항목이 위에 있습니다.
항목마다 무엇을 요청받았고, 무엇을 어떻게 쟀고, 무엇을 바꿨고, 판정이 무엇인지와 다시 돌려볼 수 있는 원문 출력을 남깁니다.

## 2026-09-03 · /check 이미지 검사 응답 시간 (OCR·1차필터·판정 배치 병렬화)

**요청** "속도가 너무 느린 것 같은데 빠르게 해줄 수 있어?" (대상: 검사(/check) 결과 나오기까지)
**잰 것** /check 파이프라인 지연 (`backend/scripts/measure_check_latency.py`: `run_check()` 직접 호출로 HTTP·이미지 캐시 우회, 단계별 시간은 몽키패치. 데모 이미지 2장, KR, 이미지만 입력(상품명·전성분 없음), CHECK_REPLACEMENTS=1, 판정 gpt-5-mini, OCR gemini-3.5-flash-lite, CHECK_MAX_WORKERS 기본 4. 이미지당 3회, 중앙값). 판정 기준은 측정 전에 못박음: VLM 실행 편차가 커서 ±10%가 아니라 **변경 후 중앙값이 기준선 최솟값보다 낮아야 개선**.

| 지표 | 전 | 후 | 변화 | 판정 |
|---|---|---|---|---|
| 선크림 데모 총 소요 (초, 문장 많은 쪽) ★ | 345.3 (332.6~353.1) | 213.6 (201.5~223.4) | −38% | 개선 확인 |
| 세럼 데모 총 소요 (초) | 202.7 (193.5~223.0) | 183.8 (162.4~190.5) | −9% | 개선 확인 |
| 선크림 RAG판정 단계 (초, 중앙값) | 187.5 | 76.1 | −59% | 개선 확인 |
| 선크림 1차필터 단계 (초, 중앙값) | 48.4 | 33.9 | −30% | 개선 확인 |
| 대체표현 단계 (초, 중앙값, 세럼/선크림) | 92.5 / 89.9 | 92.3 / 86.7 | 변화 없음 | 변화 없음 (의도, 손대지 않음) |
| findings 건수 (회차별, 세럼/선크림) | 19,18,19 / 22,21,22 | 18,19,19 / 19,22,22 | 같은 범위 | 변화 없음 |

★ 문장 수가 많아 배치가 여러 개 생기는 입력이 이 변경의 대상이라 주 지표로 둠. 두 이미지 모두 전후 범위가 겹치지 않는다. 세럼은 배치가 적어(1차필터 2~3개, 판정 2개) 이득이 작다. 남은 시간의 절반은 대체표현 단계(약 90초)인데, 이건 "한 호출로 전부"가 팀장 확정(2026-08-25, PR #391)이라 그대로 뒀다.

**바꾼 것** OCR 타일 배치(`preprocess/ocr.py`), 1차필터 배치, RAG 판정 배치(`judge/cosmetic.py`)를 `barum/parallel.py`의 `run_in_order`로 동시에 보내고 결과는 입력 순서대로 되짚는다. 프롬프트·배치 크기·모델은 그대로. `CHECK_MAX_WORKERS=1`이면 예전 순차 동작. 순서 보존·실패 격리 테스트 7건 추가(`tests/test_check_parallel.py`), 백엔드 928건 통과. 측정 스크립트는 옛 함수명(`match_rule`)으로 죽던 것을 고치고 `--out`/`--title`을 추가.
**판정** 개선 확인
**근거** `cd backend && ./venv/bin/python scripts/measure_check_latency.py --reps 3 --out <경로> --title <제목>`, 기준선 커밋 7e4fbdc (메인 워크트리, 미커밋 변경: 있음, 단 /check 경로 파일은 깨끗. origin/main 2bc6314과 /check 경로 diff 없음 확인) → 이후: 워크트리 2bc6314 + 미커밋 변경. 같은 기기·같은 .env·연달아 실행(기준선 23:51~00:19, 이후 00:37~00:59). 참고: 2026-08-23 사전측정(세럼 131초·선크림 250초)보다 이번 기준선이 느린데, 그 사이 인용 검증 게이트(PR #288)가 판정 경로에 들어갔다. 이번 항목은 같은 날 연달아 잰 전후만 비교한다.
<details><summary>원문 출력</summary>

```
before (main worktree, 7e4fbdc):
=== 세럼 (yourberry_serum_detail.png) × 3회 ===
  [1/3] 총 223.0초 (findings 19건) — OCR 13.2s / 규칙 0.0s / 1차필터 27.7s / RAG판정 70.7s / 증빙검증 0.0s / 대체표현배치 104.3s
  [2/3] 총 202.7초 (findings 18건) — OCR 10.1s / 규칙 0.0s / 1차필터 27.9s / RAG판정 69.7s / 증빙검증 0.0s / 대체표현배치 92.5s
  [3/3] 총 193.5초 (findings 19건) — OCR 9.9s / 규칙 0.0s / 1차필터 26.6s / RAG판정 80.4s / 증빙검증 0.0s / 대체표현배치 73.9s
=== 선크림 (yourberry_sunscreen_detail.png) × 3회 ===
  [1/3] 총 353.1초 (findings 22건) — OCR 20.1s / 규칙 0.0s / 1차필터 56.3s / RAG판정 165.8s / 증빙검증 0.0s / 대체표현배치 105.6s
  [2/3] 총 332.6초 (findings 21건) — OCR 15.9s / 규칙 0.0s / 1차필터 48.4s / RAG판정 188.9s / 증빙검증 0.0s / 대체표현배치 72.4s
  [3/3] 총 345.3초 (findings 22건) — OCR 14.9s / 규칙 0.0s / 1차필터 46.7s / RAG판정 187.5s / 증빙검증 0.0s / 대체표현배치 89.9s

after (worktree perf/check-parallel-batches, 2bc6314 + 변경):
=== 세럼 (yourberry_serum_detail.png) × 3회 ===
  [1/3] 총 162.4초 (findings 18건) — OCR 9.6s / 규칙 0.0s / 1차필터 13.4s / RAG판정 42.0s / 증빙검증 0.0s / 대체표현배치 93.7s
  [2/3] 총 183.8초 (findings 19건) — OCR 10.3s / 규칙 0.0s / 1차필터 13.9s / RAG판정 65.9s / 증빙검증 0.0s / 대체표현배치 91.4s
  [3/3] 총 190.5초 (findings 19건) — OCR 10.0s / 규칙 0.0s / 1차필터 13.5s / RAG판정 72.4s / 증빙검증 0.0s / 대체표현배치 92.3s
=== 선크림 (yourberry_sunscreen_detail.png) × 3회 ===
  [1/3] 총 223.4초 (findings 19건) — OCR 11.5s / 규칙 0.0s / 1차필터 44.7s / RAG판정 77.7s / 증빙검증 0.0s / 대체표현배치 85.2s
  [2/3] 총 201.5초 (findings 22건) — OCR 12.0s / 규칙 0.0s / 1차필터 18.4s / RAG판정 66.2s / 증빙검증 0.0s / 대체표현배치 99.7s
  [3/3] 총 213.6초 (findings 22건) — OCR 11.5s / 규칙 0.0s / 1차필터 33.9s / RAG판정 76.1s / 증빙검증 0.0s / 대체표현배치 86.7s
```
</details>

